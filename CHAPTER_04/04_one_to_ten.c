#include <stdio.h>
int main()
{

    // Here i stands for iterator or counter variable
    // for(initialisation; condition; updation);
    for(int i = 0; i<=10; i++)
    {
        printf("%d \n",i);
    }

    return 0;
}